extends mix.jade

block main
  .sheet
    .card
      table
        caption 
          button(style="border:none;padding:0;background:none" onclick="alert('Hi im carl to u')") Carl
    .card
      table
        caption Anna
  form
    input(required type="date", name="adate")#atime
    input(required type="time", name="atime")#adate
    button Go

  hr

  +alert("success",null,"hshs")
  +alert("info","You are the onli")
  +alert("danger","Its always been you","VOLDDYMORT")

  hr

  p FORM
  form(
  action="new"
  style="display:none"
  onsubmit="alert('hi i am submitted');"
  )#testForm
    input(name="color")
    button click to submit
    input(type='submit')

  p BUTTON

  button#mybutt(
  data-toggle="modal"
  data-target="#momo"
  data-who="You Got Me"
  ).btn.btn-info Launch modal

  input(type='submit')

  button(
  form="testForm"
  onclick="document.querySelector('#testForm').style.display='block';event.preventDefault();"
  ).btn.btn-danger Sumotter

  p MODAL
  .modal.fade#momo
    .modal-dialog
      .modal-content
        .modal-header
          button.close(data-dismiss="modal") x
          h4.modal-title whatever
        .modal-body
          form(action="new")
            input(required type="date", name="adate")#atime
            input(required type="time", name="atime")#adate
            input#pname(name="pname" value="whoami")
            button.btn.btn-success Gooo!
        .modal-footer
          button.btn.btn-danger.btn-sm(data-dismiss="modal") Close

  script.
    var mybutt = document.querySelector("#mybutt");
    mybutt.addEventListener("click", function(e) {
      var who = e.target.dataset.who
      alert(who)
      var pname = document.querySelector("#pname")
      pname.value = who
    });
  +alert("warning","The End")
